/**
 * Stitch mockup copy is DISABLED.
 * Legal OS runtime is React under apps/web/src/components/os/.
 * Historical Stitch HTML is not required for build/dev.
 *
 * To permanently remove uploads from git (run once on a machine with the repo):
 *   bash scripts/purge-stitch-uploads.sh
 */
console.log(
  "[copy-mockups] skipped — Stitch uploads retired; use React Legal OS pages.",
);
