# acquit-ai-worker (canonical)

Cloudflare reverse proxy for the Acquit API.

```bash
cd workers/acquit-ai-worker
npx wrangler deploy
```

**Do not deploy** `acquit-ai-worker/` at repo root or `acquit-platform/acquit-ai-worker/` — those are deprecated duplicates.
